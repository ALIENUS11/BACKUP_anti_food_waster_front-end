# 前端开发约定（看这一页就够了）

> 详细设计规范见上级目录 `UI设计规范.md`。本页只讲**怎么用**。
> 主题：**浅色**（明亮展厅）

---

## 0、先打开样板页看一眼

```
npm run dev
浏览器打开  http://localhost:3000/template
```

样板页在 **`src/pages/Template/index.tsx`**，数据在 **`src/mock/template.ts`**。

这一页把一个标准页面的所有组成部分演示了一遍：页面骨架、指标卡、图表、表格、风险标签、按钮、点击反馈、数据怎么放。**写自己的模块时，照着它抄结构。**

样板页的注释里逐段标了「这里是什么、什么时候用」，看完基本就会了。

---

## 一、主题已经配好了，你什么都不用配


| 文件 | 作用 |
| --- | --- |
| `src/index.css` | **设计令牌**。全项目唯一的颜色 / 字体来源，改这里 11 个页面一起变 |
| `src/theme/antd.ts` | 把令牌同步给 Ant Design 组件（Table / Modal / Select 等自动就是项目主色） |
| `src/theme/echarts.ts` | ECharts 统一图表主题 |
| `src/components/` | 通用组件，直接用，不要改 |

---

## 二、写页面时的三条铁律

1. **禁止写 `#` 色值**。颜色只能从令牌取（下面那张表）。
2. **禁止用 Tailwind 默认色**（`bg-blue-500`、`text-gray-400` 这类）。项目已经把它们**全部清空**了，你写了也不会生效。
3. **禁止自己装 UI 库**。只用 Ant Design + Tailwind。

---

## 三、色名对照表（背不下来就查这里）

| 你要什么 | 写这个类名 | 色值 |
| --- | --- | --- |
| **背景** | | |
| 页面底（浅灰） | `bg-canvas` | `#F5F7FA` |
| 卡片 / 面板（白） | `bg-panel` | `#FFFFFF` |
| 表头 / 选中态 | `bg-raised` | `#EEF2F7` |
| 悬停态 | `bg-hover` | `#E8EEF6` |
| **文字** | | |
| 标题、关键数值 | `text-ink` | `#0F172A` |
| 正文、说明 | `text-ink-muted` | `#475569` |
| 单位、时间戳、口径说明 | `text-ink-subtle` | `#64748B` |
| 禁用 | `text-ink-disabled` | `#CBD5E1` |
| **主色**（全项目唯一） | `text-brand` / `bg-brand` / `border-brand` | `#1668DC` |
| 主色浅底 | `bg-brand-soft` | 8% 透明 |
| **风险三色**（与沙盘 LED 灯带色相一致） | | |
| 安全 / 低风险 | `text-safe` / `bg-safe-soft` | `#16A34A` |
| 预警 / 中风险 | `text-warn` / `bg-warn-soft` | `#D97706` |
| 风险 / 高风险 | `text-danger` / `bg-danger-soft` | `#DC2626` |
| **边框** | `border-line` / `border-line-strong` | `#E3E8EF` / `#CBD5E1` |
| **图表色序** | `text-chart-1` … `text-chart-6` | 见规范 |

**字号**：`text-hero`(72px 大数字) / `text-display`(32px 页面标题) / `text-section`(24px 区块标题) / `text-card`(18px 卡片标题)，正文用 `text-base`(16px)，辅助用 `text-sm`(14px)，标签用 `text-xs`(12px)。

**光影**：卡片用 `shadow-panel`，激活/高亮用 `shadow-glow`。**浅色主题必须用阴影**，否则卡片会和背景糊在一起。

**数字**：大号指标数字加 `font-mono`，保证纵向对齐。

---

## 三点五、目录结构 & 怎么加一个新页面

### 目录结构：一个区域一个文件夹

```
src/
  pages/
    Home/                 首页导航
    Template/             开发样板页（看这个学怎么写）
    hall1/                区域一 · 序厅
      Route/                1-1 一粒米全链路动态路径展示
    hall2/                区域二 · 收储
      FieldLoss/            2-1 田间收获减损展示
      Impurity/             2-2 杂质筛除系统
      Quality/              2-3 粮食品质综合分级系统
    hall3/                区域三 · 运输
      RoutePlan/            3-1 运输路线选择系统
      RiskForecast/         3-2 运输风险预测系统
    hall4/                区域四 · 仓储
      Pest/                 4-1 虫害环境模拟系统
      Decision/             4-2 AI仓储风险综合决策系统
    hall5/                区域五 · 消费
      CanteenWarning/       5-1 数字化食堂浪费场景警示展示
      PlateDetect/          5-2 餐盘剩余物智能识别系统
    hall6/                区域六 · 尾厅
      Overview/             6-1 全链条成果动态总览系统
  router/
    routes.ts             ★ 唯一的路由表
  mock/
    hallN/xxx.ts          对应模块的假数据
  components/             通用组件（王宇烨维护，只用不改）
  theme/                  AntD / ECharts 主题
```

**为什么模块也是一个文件夹**（而不是一个 `.tsx` 文件）：一个模块页面往往几百行，还会有自己的子组件、hook、类型。放在同一个文件夹里，不会污染区域文件夹，也不用把这些东西塞进全局 `components/`。

### 怎么加一个新页面（两步）

**第 1 步：建页面文件**

在对应区域文件夹下建 `<模块名>/index.tsx`。例如新加一个「粮食追溯」到收储区域：

```
src/pages/hall2/Traceability/index.tsx
```

内容照抄样板页的结构（`src/pages/Template/index.tsx`），最小骨架长这样：

```tsx
import { PageShell } from '@/components'

const Traceability = () => (
  <PageShell area="收储" title="粮食追溯">
    {/* 内容… */}
  </PageShell>
)

export default Traceability
```

**第 2 步：在路由表加一条**

打开 **`src/router/routes.ts`**，找到对应区域，往 `modules` 数组里加一行：

```ts
{
  area: '收储',
  code: 'hall2',
  device: '粮库入库质检微缩实景沙盘设备',
  modules: [
    // …原有条目…
    {
      num: '2-4',                                  // 模块号
      name: '粮食追溯',                             // 页面顶部栏显示的名字
      path: '/hall2/traceability',                 // 访问地址
      owner: '熊欣',                                // 负责人
      Component: Hall2Traceability,                // 页面组件
    },
  ],
},
```

别忘了在文件顶部的 import 区加一行（保持按区域分组）：

```ts
import Hall2Traceability from '@/pages/hall2/Traceability'
```

**就这两步。** `App.tsx` 和首页导航都会自动读取路由表，**不需要改别的地方**，首页会自动多出这个入口。

### 路由命名约定

| 规则 | 例子 |
| --- | --- |
| 地址 = `/区域文件夹/短横线命名的模块名` | `/hall2/impurity`、`/hall4/pest` |
| 目录名用大驼峰 | `Impurity`、`PlateDetect` |
| 地址用短横线小写 | `/hall2/field-loss`、`/hall5/plate-detect` |

地址里的 `hallN` 就是模块号的前半段，看到 `/hall4/pest` 就知道是 **4-1**。

---

## 四、优先用通用组件，而不是自己 div 套 div

```tsx
import { PageShell, Panel, MetricCard, RiskTag } from '@/components'

<PageShell area="收储" title="杂质筛除系统" status={<RiskTag level="warn" />}>
  <div className="grid grid-cols-12 gap-6">
    <Panel title="粮食表面特征分析" className="col-span-8">
      ...
    </Panel>
    <div className="col-span-4 space-y-6">
      <MetricCard label="杂质率" value="2.10" unit="%" tone="warn"
                  hint="杂质数量 ÷ 总颗粒数 × 100%" />
      <MetricCard label="正常颗粒数" value="1834" />
    </div>
  </div>
</PageShell>
```

| 组件 | 什么时候用 |
| --- | --- |
| `PageShell` | **每个页面都用**，保证顶部栏结构一致 |
| `Panel` | 任何内容块。**所有内容都要包在 Panel 里，不要裸放** |
| `MetricCard` | 展示单个关键数值 |
| `RiskTag` | **唯一的**风险等级显示方式，禁止自己写红黄绿圆点 |

---

## 五、图表：一句 theme 就够

```tsx
import ReactECharts from 'echarts-for-react'

<ReactECharts theme="grain-hall" option={option} style={{ height: 320 }} />
```

加上 `theme="grain-hall"` 之后，网格线、坐标轴、图例、Tooltip 自动统一，**不要在 option 里重复写这些样式**。

---

## 六、常见错误

| ❌ 错误写法 | ✅ 正确写法 |
| --- | --- |
| `bg-[#FFFFFF]` | `bg-panel` |
| `text-gray-500` | `text-ink-muted` |
| `bg-blue-500` | `bg-brand` |
| `border` (默认色) | `border border-line` |
| `shadow-md` | `shadow-panel` |
| `<span className="w-2 h-2 bg-red-500 rounded-full" />` | `<RiskTag level="danger" />` |
| 自己写 `<div className="rounded-xl bg-white p-6">` | `<Panel>` |

---


